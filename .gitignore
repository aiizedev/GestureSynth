# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# Variables de entorno. `.env.local` ya lo cubre `*.local`; esto ignora el resto
# pero conserva la plantilla versionada.
.env
.env.*
!.env.example

# Assets de MediaPipe: se obtienen con `npm run setup:mediapipe` (postinstall).
# ~42 MB de wasm + ~7 MB de modelo, no van al repo.
public/mediapipe/

# Evidencia de `archify visual-check` (no se despliega).
public/diagrams/*.visual-check.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local scratch (plans, machine-local settings)
.claude/
