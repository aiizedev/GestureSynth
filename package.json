{
  "name": "gesture-synth",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "setup:mediapipe": "node scripts/fetch-mediapipe.mjs",
    "postinstall": "node scripts/fetch-mediapipe.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1",
    "tone": "^15.0.4"
  },
  "devDependencies": {
    "happy-dom": "^20.12.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
