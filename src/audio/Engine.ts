/**
 * STUB — todavía no implementado.
 *
 * TODO (roadmap CLAUDE.md): orquestador del lado de audio. Cuando exista el loop
 * de tracking + render a 30–60fps, `Engine` será el dueño del `Synth`, del bucle
 * de audio y del ciclo de vida (`Tone.start()`, transporte, presets activos).
 *
 * Por ahora `main.ts` instancia `Synth` directamente; esta pieza entra cuando se
 * conecte `tracking/HandTracker.ts`.
 */
export {};
